{
  "name": "request-retry",
  "version": "0.0.1",
  "description": "Segment.io retrying request layer",
  "author": "Segment.io <friends@segment.io>",
  "engines": {
    "node": ">= 0.8"
  },
  "main": "./lib",
  "scripts": {
    "test": "mocha --reporter spec"
  },
  "dependencies": {
    "request": "git://github.com/segmentio/request#no-agent",
    "defaults": "~1.0.0",
    "debug": "~0.7.2"
  },
  "devDependencies": {
    "should": "*",
    "mocha": "*",
    "exporess": "*"
  }
}
